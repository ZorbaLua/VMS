#include <stdio.h>

#include "heap.h"

extern Heap heap;

extern void try(int);


int Heap_free(heapPt pt){ return -1; }

void Heap_getBlock(heapPt pt, char* ret){ }

heapPt Heap_alloc(char *s, int len){
heapPt hp=0;
return hp;
}

void Heap_init(int size){ }

